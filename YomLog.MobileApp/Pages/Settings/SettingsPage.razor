@page "/settings"

@inject LayoutService LayoutService

<PageContainer Title="設定" MaxWidth="MaxWidth.Small">
    <MudExpansionPanels>
        <MudExpansionPanel Text="外観の設定" IsExpanded="true">
            <MudList>
                <MudListItem>
                    <MudSwitch @bind-Checked="@_isDarkMode" @onclick="LayoutService.ToggleDarkMode"
                               Color="Color.Primary" Label="ダークモード" />
                </MudListItem>
            </MudList>
        </MudExpansionPanel>
    </MudExpansionPanels>
</PageContainer>

@code {
    private bool _isDarkMode;

    protected override void OnInitialized()
    {
        _isDarkMode = LayoutService.IsDarkMode.Value;
    }
}