@inject IJSRuntime JSRuntime

<PageContainer LeftButton="AppBarLeftButton.Back">
    <MudText Typo="Typo.h3" GutterBottom="true">404 Not Found</MudText>
    <MudText Class="my-8">The item you are looking for was not found.</MudText>

    <MudButton OnClick=@(() => JSRuntime.InvokeVoidAsync("history.back"))
               Color="Color.Primary"
               Size="Size.Large"
               Variant="Variant.Filled">
        Back
    </MudButton>
</PageContainer>