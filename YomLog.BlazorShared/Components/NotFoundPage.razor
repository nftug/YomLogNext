@inject IJSRuntime JSRuntime

<PageContainer LeftButton="AppBarLeftButton.Back" Title="404 Not Found">
    <MudText Typo="Typo.h3" GutterBottom="true">404 Not Found</MudText>
    <MudText Class="my-8">お探しのアイテムは見つかりません。</MudText>

    <MudButton OnClick=@(() => JSRuntime.InvokeVoidAsync("history.back"))
               Color="Color.Primary"
               Size="Size.Large"
               Variant="Variant.Filled">
        戻る
    </MudButton>
</PageContainer>